# Branchement Supabase/Epicollect

Après avoir renseigné les variables Vercel, lancer les routes `/api/sync/...`.

Les données sont insérées ou mises à jour dans Supabase via `source_entry_id`.

Les mappers peuvent être ajustés si les libellés exacts des exports réels diffèrent.

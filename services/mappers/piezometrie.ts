import { getValue, findLocation, safeNumber, sourceId } from "../epicollect/client";
export function mapPiezometre(entry:any){const loc=findLocation(entry);return {code_piezo:getValue(entry,["Code piézomètre","Code piezometre","Code / Numéro du piézomètre"])||sourceId(entry),latitude:loc.latitude,longitude:loc.longitude,profondeur:safeNumber(getValue(entry,["Profondeur totale (m)","Profondeur"])),aquifere:getValue(entry,["Nom de l'aquifère capté","Nom de l'aquifère","Aquifère"]),source_entry_id:sourceId(entry),synced_at:new Date().toISOString()};}
export function mapPiezoObservation(entry:any){return {date_observation:getValue(entry,["Date de mesure","Date","Date observation"]),niveau_statique:safeNumber(getValue(entry,["Niveau statique (m)","Niveau statique","Niveau d'eau"])),observateur:getValue(entry,["Observateur","Nom observateur"]),commentaire:getValue(entry,["Commentaires","Commentaire"]),photo_url:getValue(entry,["Photo de mesure","Photo du site","Photo"]),source_entry_id:sourceId(entry),synced_at:new Date().toISOString()};}

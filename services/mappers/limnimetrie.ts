import { getValue, findLocation, safeNumber, sourceId } from "../epicollect/client";
export function mapLimniStation(entry:any){const loc=findLocation(entry);return {code_station:getValue(entry,["Code station","Code échelle","Village"])||sourceId(entry),cours_eau:getValue(entry,["Nom cours d’eau","Nom cours d'eau","Cours d'eau","Nom du cours d'eau"]),latitude:loc.latitude,longitude:loc.longitude,source_entry_id:sourceId(entry),synced_at:new Date().toISOString()};}
export function mapLimniObservation(entry:any){return {date_observation:getValue(entry,["Date lecture","Date de lecture","Date"]),periode:getValue(entry,["Période","Periode","Moment"])||null,hauteur_eau:safeNumber(getValue(entry,["Niveau d’eau observé","Niveau eau","Hauteur eau","Hauteur d'eau"])),commentaire:getValue(entry,["Commentaire technique","Observation générale","Commentaire"]),photo_url:getValue(entry,["Photo lecture du jour","Photo"]),source_entry_id:sourceId(entry),synced_at:new Date().toISOString()};}

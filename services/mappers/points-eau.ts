import { getValue, findLocation, safeNumber, sourceId } from "../epicollect/client";
export function mapPointEau(entry:any){const loc=findLocation(entry);return {code_pe:getValue(entry,["Code PE","Code point d'eau","Village","Localité","Localite"])||sourceId(entry),type_ouvrage:getValue(entry,["Type d'ouvrage","Type ouvrage","Type de point d'eau","Type point d'eau"]),latitude:loc.latitude,longitude:loc.longitude,profondeur:safeNumber(getValue(entry,["Profondeur","Profondeur totale"])),etat:getValue(entry,["Etat","État","Fonctionnalité","Fonctionnalite","Etat de fonctionnement"]),source_entry_id:sourceId(entry),synced_at:new Date().toISOString()};}

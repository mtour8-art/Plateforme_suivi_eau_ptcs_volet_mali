"use client";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";
export function useRole(){const[role,setRole]=useState("Public");const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();const t=data.session?.access_token;if(!t){setLoading(false);return}const r=await fetch("/api/auth/me",{headers:{Authorization:`Bearer ${t}`}});const j=await r.json();setRole(j.role||"Public");setLoading(false)})()},[]);return{role,loading,canManage:role==="Administrateur PTCS",canOperate:role==="Administrateur PTCS"||role==="DNH/DRHK"}}

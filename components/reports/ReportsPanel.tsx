"use client";import {useState} from "react";import {useRole} from "@/components/auth/useRole";
export default function ReportsPanel(){const{role,canOperate}=useRole();const[module,setModule]=useState("global"),[periode,setPeriode]=useState("personnalisee");return <div className="panel"><h2>Rapports et exports dynamiques</h2><p><span className="role-badge">Rôle : {role}</span></p>{!canOperate&&<div className="notice-empty">Rapports et exports réservés à Administrateur PTCS et DNH/DRHK.</div>}<div className="grid-2"><select className="input" value={module} onChange={e=>setModule(e.target.value)}><option value="global">Rapport global</option><option value="pluviometrie">Pluviométrie</option><option value="piezometrie">Piézométrie</option><option value="limnimetrie">Limnimétrie</option><option value="points_eau">Points d'eau</option></select><select className="input" value={periode} onChange={e=>setPeriode(e.target.value)}><option value="personnalisee">Période personnalisée</option><option value="mensuel">Mensuel</option><option value="trimestriel">Trimestriel</option><option value="annuel">Annuel</option></select></div><div className="report-grid" style={{marginTop:16}}>{["pdf","docx","xlsx","csv"].map(f=><a key={f} className={canOperate?"btn btn-primary":"btn btn-primary disabled-action"} href={canOperate?`/api/reports/export?module=${module}&format=${f}&periode=${periode}`:"#"} target="_blank">Exporter {f.toUpperCase()}</a>)}</div></div>}

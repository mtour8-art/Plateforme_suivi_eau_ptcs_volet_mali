"use client";import {useEffect,useState} from "react";
export default function UserCreateForm(){const[roles,setRoles]=useState<any[]>([]),[email,setEmail]=useState(""),[roleId,setRoleId]=useState(""),[nom,setNom]=useState(""),[prenom,setPrenom]=useState(""),[msg,setMsg]=useState("");useEffect(()=>{fetch("/api/admin/users").then(r=>r.json()).then(j=>setRoles(j.roles||[]))},[]);async function submit(e:React.FormEvent){e.preventDefault();setMsg("Envoi...");const r=await fetch("/api/admin/users/create",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,role_id:roleId,nom,prenom})});const j=await r.json();setMsg(j.ok?j.message:"Erreur : "+j.error)}return <div className="panel"><h2>Ajouter un utilisateur</h2><p style={{color:"#64748b"}}>L'utilisateur reçoit un lien pour créer son mot de passe.</p><form onSubmit={submit}><input className="input" placeholder="Prénom" value={prenom} onChange={e=>setPrenom(e.target.value)}/><input className="input" placeholder="Nom" value={nom} onChange={e=>setNom(e.target.value)}/><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><select className="input" value={roleId} onChange={e=>setRoleId(e.target.value)} required><option value="">Choisir le rôle</option>{roles.map(r=><option key={r.id} value={r.id}>{r.nom_role}</option>)}</select><button className="btn btn-primary" style={{marginTop:14}}>Envoyer l'invitation</button></form>{msg&&<p><strong>{msg}</strong></p>}</div>}

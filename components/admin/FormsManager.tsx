"use client";
import { useEffect, useState } from "react";
export default function FormsManager(){const [forms,setForms]=useState<any[]>([]);const [source,setSource]=useState("");useEffect(()=>{fetch("/api/admin/forms").then(r=>r.json()).then(j=>{setForms(j.data||[]);setSource(j.source||"")})},[]);return <div className="panel"><h2>Formulaires Epicollect5</h2><p style={{color:"#64748b"}}>Source : {source}. Les liens restent disponibles même avant synchronisation.</p><table className="table"><thead><tr><th>Module</th><th>Formulaire</th><th>Type</th><th>Accès</th></tr></thead><tbody>{forms.map((f,i)=><tr key={i}><td>{f.module}</td><td>{f.libelle}</td><td>{f.type_source}</td><td><a className="btn btn-soft" href={f.form_url} target="_blank">Ouvrir</a></td></tr>)}</tbody></table></div>}

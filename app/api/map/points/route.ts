export const dynamic = "force-dynamic";
export const revalidate = 0;
export const fetchCache = "force-no-store";
import { NextResponse } from "next/server";import { supabaseAdmin } from "@/lib/supabase-admin";
const fallback=[{module:"pluviometrie",code:"PLV-001",libelle:"Station pluviométrique",latitude:12.86,longitude:-7.56},{module:"piezometrie",code:"PZ-001",libelle:"Piézomètre",latitude:12.88,longitude:-7.50},{module:"limnimetrie",code:"LIM-001",libelle:"Station limnimétrique",latitude:12.80,longitude:-7.62},{module:"points_eau",code:"PE-001",libelle:"Point d'eau",latitude:12.91,longitude:-7.47}];
function valid(r:any){return r&&r.latitude!==null&&r.longitude!==null&&Number.isFinite(Number(r.latitude))&&Number.isFinite(Number(r.longitude))}
export async function GET(){const {data:view}=await supabaseAdmin.from("v_carte_points").select("*").limit(5000);if(view?.some(valid))return NextResponse.json({ok:true,data:view.filter(valid),source:"view"});const out:any[]=[];const cfg=[["stations_pluvio","pluviometrie","code_station","nom_station"],["piezometres","piezometrie","code_piezo","code_piezo"],["stations_limni","limnimetrie","code_station","cours_eau"],["points_eau","points_eau","code_pe","type_ouvrage"]];for(const [table,module,code,label] of cfg){const {data}=await supabaseAdmin.from(table).select("*").limit(2000);for(const r of data||[]){if(valid(r))out.push({module,code:r[code],libelle:r[label],latitude:r.latitude,longitude:r.longitude,synced_at:r.synced_at})}}return NextResponse.json({ok:true,data:out.length?out:fallback,source:out.length?"tables":"fallback"})}
